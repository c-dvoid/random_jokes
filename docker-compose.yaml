
services:
  app:
    build:
      context: ./backend
    expose:
      - "8000"
    ports:
      - "8000:8000"

  nginx:
    build:
      context: ./frontend
    ports:
      - "80:80"
    depends_on:
      app:
        condition: service_started